class Note{
  String noteID;
  String noteTitle;
  String noteText;

  DateTime createDateTime;
  DateTime lastEditDateTime;

  Note({
    this.noteID, this.noteTitle, this.noteText,this.createDateTime, this.lastEditDateTime});
}